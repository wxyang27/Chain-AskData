[project]
name = "chain-askdata"
version = "0.2.0"
description = "新氧连锁经管自然语言取数 MVP"
requires-python = ">=3.14"
dependencies = [
  "chromadb",
  "fastapi",
  "openpyxl",
  "pytest",
  "python-docx",
  "python-dotenv",
  "uvicorn",
]

[tool.pyright]
include = ["app", "tests"]
